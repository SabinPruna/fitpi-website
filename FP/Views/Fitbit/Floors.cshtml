@using FP.Models.Fitbit
@model List<FP.Models.Fitbit.FloorsModel>

@{
    ViewBag.Title = "Fitpi - Floors";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@section Scripts
{
    <script src="@Url.Content("~/Scripts/modules/floorsChart.js")"></script>
}

@using (Html.BeginForm("Floors", "Fitbit", FormMethod.Post, new { @class = "form-floors" }))
{

    <div class="row pt-3">
        <div class="col-4">
            <div class="table-wrapper-scroll-y scrollable-floors-table">
                <table class="table table-floors">
                    @for (int i = 0; i < Model.Count; i++)
                    {
                        <tr style="height: 40px">
                            <td>
                                @Html.LabelFor(modelItem => Model[i].Date, Model[i].Date, new { @class = "fz-22" })
                                @Html.HiddenFor(modelItem => Model[i].Date, Model[i].Date)
                            </td>
                            <td>
                                @Html.TextBoxFor(m => Model[i].Summary.Floors, Model[i].Summary.Floors.ToString(), new { @class = "form-control floors-control pull-right" })
                            </td>
                        </tr>
                    }
                </table>
            </div>

        </div>
        <div class="col-8">
            <div class="chart-background" style="width: 1200px; height: 700px">
                <canvas id="floorsChart"></canvas>
            </div>
        </div>
        <div class="col-2">
            <button type="submit" class="btn btn-primary btn-extra-large chart-btn">Save</button>
        </div>
    </div>

}